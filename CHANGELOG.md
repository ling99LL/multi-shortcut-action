# Changelog

## 1.3.1 - 2026-09-08

- 修复 V4.x PCB 源码使用字符串枚举（`NONE`/`OBSTRUCT`/`SURROUND&PUSH`）时切换无效的问题。
- 保持源码原有的数字或字符串表示；`Shift + R` 仍只在阻挡和忽略之间往返，其他模式首次进入阻挡。
- 增加字符串枚举格式的回归测试。

## 1.3.0 - 2026-09-08

- 修复在当前 EasyEDA 客户端中快捷键无效的问题：兼容运行时实际提供的旧版 `registerShortcutKey`，并保留新版 ID 注册接口支持。
- 增加快捷键注册模式、逐项注册结果和失败原因的状态诊断。
- 将 `Shift + R` 的布线模式切换目标改为“阻挡/忽略”；阻挡与忽略互相切换，其他模式首次按下先进入阻挡。
- 更新菜单、说明文档、多语言资源和烟测。

## 1.2.0 - 2026-09-08

- 将布线模式快捷切换目标改为“阻挡/环绕”，默认快捷键改为 `Shift + R`。
- 增加默认关闭且可持久化的开关；开启后快捷键只在阻挡和环绕之间切换，关闭时不执行本扩展的切换动作。
- 使用 `SYS_Storage` 保存开关状态，并更新菜单、状态对话框、说明文档、多语言资源和烟测。
- 明确记录系统快捷键优先以及 `Shift + R` 冲突时不能被扩展覆盖的限制。

## 1.1.0 - 2026-09-08

- 增加仅在 PCB 编辑器生效的“切换布线冲突模式（阻挡/忽略）”快捷键。
- 默认快捷键为 `Ctrl + Alt + Shift + F11`，可在嘉立创EDA快捷键设置中修改或清除。
- 通过官方文档源码接口更新当前 PCB 文档的 `PREFERENCE.routingMode`；推挤/环绕模式按下后先切换为阻挡。
- 增加 PCB 菜单入口、状态提示、错误提示、多语言资源和源码格式测试。

## 1.0.0 - 2026-09-03

- 新建独立的“多快捷键动作”扩展。
- 使用两个唯一快捷键 ID 注册同一个共享动作函数。
- 增加快捷键状态查询、错误反馈和会话内执行计数。
- 增加简体中文与英文多语言资源。
- 增加原创插件图标、使用说明和 EDA v4.2/BETA 边界说明。
